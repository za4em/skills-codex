interface:
  display_name: "vibe-coder"
  short_description: "Implement next checklist step from spec and plan"
  default_prompt: "Read docs/spec.md and docs/plan.md, implement the next unchecked checklist item within defined plan boundaries using existing codebase patterns/components, verify against spec acceptance criteria, and mark the completed plan item as done. If all plan items are complete, advise: clear context/start a new session, then run $code-reviewer."
